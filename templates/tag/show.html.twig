{% extends 'base.html.twig' %}

{% block title %}
    {{ 'tag.tag'|trans({'%id%': tag.id|default('')}) }}
{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="user-details">{{ 'tag.tag'|trans({'%id%': tag.id|default('')}) }}</h1>
    {% if tag is defined and tag|length %}

            <h1 class="user-text">{{ 'id.id'|trans }}</h1>
            <h1 class="user-data">{{ tag.id }}</h1>
            <h1 class="user-text">{{ 'title.title'|trans }}</h1>
            <h1 class="user-data">{{ tag.title }}</h1>


        <h2 class="user-details">{{ 'Songs.with.this.tag'|trans }}</h2>
        {% if tag.tasks is not empty %}
            <ul>
                {% for task in tag.tasks %}
                    <li class="user-data">{{ task.title }}</li>
                {% endfor %}
            </ul>
        {% else %}
            <p>{{ 'message.empty_list'|trans }}</p>
        {% endif %}

        <p>
            <a class="back-btn" href="{{ url('tag_index') }}" title="{{ 'back.to.list'|trans }}">
                {{ 'back.to.list'|trans }}
            </a>
        </p>
        </div>
    {% else %}
        <p>
            {{ 'message.empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}
