{% include 'default/_form.html.twig' with {
    page_title: 'register.log'|trans,
    attr: {
        method: 'POST',
        action: url('app_register')
    },
    form: form,
    submit_label: 'register.log'|trans,
    back_to_list_path: 'app_login',
} %}